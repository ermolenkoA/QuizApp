package com.example.quizapp.utils

import com.example.quizapp.data.Topic

object Utils {
    val dataset = arrayListOf<Topic>(
        Topic(
            id = 0,
            title = "Animals",

        )
    )
}